# scratch / local
.claude/
__pycache__/
*.pyc
.DS_Store
*.log
.venv/

# scraped video cache for scripts/scrape_bilibili_cn.py
.bilicache/

# local Qimai session cookies for the daily fetch (cookie 1 = HoYo, cookie 2 = the rest)
# — NEVER commit these
scripts/.qimai_cookie*

# CN scrape working files (the merged data/cn_revenue.json + cn_monthly.json are committed)
data/cn_shards/
data/cn_verify/
